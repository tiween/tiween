import { Meta } from '@storybook/react/types-6-0';
import SmallMovieCard from '../components/Movie/SmallMovieCard';

export default { title: 'Small Movie Card', component: SmallMovieCard } as Meta;

// export const withShowtimes: Story = () => <SmallMovieCard movie={sample(movies)} shows={shows} />;
// export const withOutShowtimes: Story = () => (
//   <SmallMovieCard movie={sample(movies)} showTimeTable={false} />
// );
