// Per-file setup for the "admin" Jest project (jsdom environment).
// Extends Jest's expect with @testing-library/jest-dom matchers
// (toBeInTheDocument, toHaveAttribute, ...).
import "@testing-library/jest-dom"
