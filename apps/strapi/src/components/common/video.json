{
  "collectionName": "components_common_videos",
  "info": {
    "displayName": "Video",
    "icon": "file-video",
    "description": "Video link with type classification"
  },
  "options": {},
  "attributes": {
    "url": {
      "type": "string",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "FULL_LENGTH",
        "TEASER",
        "CLIP"
      ],
      "default": "TEASER",
      "required": false
    },
    "videoType": {
      "type": "enumeration",
      "enum": [
        "trailer",
        "teaser",
        "clip",
        "featurette",
        "interview",
        "behind-the-scenes",
        "full-length"
      ],
      "default": "trailer",
      "required": false
    }
  }
}
