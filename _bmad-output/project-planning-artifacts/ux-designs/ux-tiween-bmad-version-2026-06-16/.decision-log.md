# Decision Log — Tiween B2C UX (Design Handoff mode)

Run: ux-tiween-bmad-version-2026-06-16 · Facilitator: Sally (bmad-ux) · User: Ayoub

Spines win on conflict with any mock, import, or upstream doc. This log is canonical for decisions; DESIGN.md / EXPERIENCE.md are the distilled contracts.

---

## Session framing

- **Scope:** B2C client app (full) — consumer Next.js PWA. (Strapi Admin / B2B out of scope this run.)
- **Working mode:** Design handoff — capture discovery → assemble producer-shaped prompt for external visual-identity tool → user runs it → EXPERIENCE.md follows via Update mode.
- **UI system inherited:** shadcn/ui + Tailwind v4 (`@theme`), Next.js 15 App Router, RSC-default. Both spines extend this system; DESIGN.md tokens reference/extend shadcn defaults.

## Sources inherited (by reference — not duplicated)

- `_bmad-output/project-planning-artifacts/ux-design-specification.md` — **primary**. Legacy monolithic UX spec (2025-12-25), 1804 lines, built from the brand book + 28 interface mockups. Visual identity extracted into `.working/visual-identity-extract.md`.
- `_bmad-output/project-planning-artifacts/product-brief-tiween-bmad-version-2025-12-25.md`
- `_bmad-output/prd/` (sharded PRD)
- Brand assets cited by the legacy spec (not yet located on disk): `brandbook_tiween.pdf`, `tiween_compo.pdf` (logo variations), `reponses_brief_crea.pdf` (creative brief), `input/TIWEEN_*.png` (28 mockups).

## Visual identity — LOCKED (from legacy spec, brand-book-derived)

- **Direction:** "Dark Cinema Aesthetic with Yellow Accent Strategy." Single dark theme (no light-mode toggle). Poster-forward, card-based, entertainment-app conventions.
- **Colors:** Tiween Green `#032523` (primary bg/surface), Tiween Yellow `#F8EB06` (accent/CTA/focus), White `#FFFFFF` (text), Surface Light `#0A3533`, Surface Lighter `#0F4542`. Semantic: destructive `#EF4444`, success `#22C55E`, warning `#F59E0B`, muted-foreground `#A0A0A0`.
- **Typography:** Lalezar (display, AR+Latin headlines) · Inter (Latin body/UI) · Noto Sans Arabic (AR body/UI) · JetBrains Mono (ticket IDs/technical). Min 16px base, 1.5 line-height.
- **Spacing/grid:** 4px base scale; mobile 4-col/16px, tablet 8-col/24px, desktop 12-col/24px max 1280px. Tailwind breakpoints. Touch targets 44px min / 48px preferred.
- **Elevation expressed via surface-color shift, not shadow.** Visual hierarchy L1 hero → L2 Lalezar section headers → L3 Inter card titles → L4 secondary text.
- **Components:** EventCard, ShowtimeButton, TicketQR, BottomNav (4 tabs), SeatSelector — anatomies specified in extract. Button hierarchy: Primary yellow (48px), Secondary outline (44px), Tertiary ghost (40px), Destructive red.
- **A11y:** WCAG 2.1 AA. Yellow-on-teal = 12.5:1, white-on-teal = 15.8:1. 3px yellow focus outline + 2px offset. Status = icon + text, never color alone.
- **i18n/RTL:** AR/FR/EN, Arabic-first then adapt to LTR. Logical properties, mirrored directional icons, instant flip on switch. Pricing in DT. FR-default UI copy.

## Brand assets located + read (input/ → copied to imports/)

`brandbook_tiween.pdf` (20pp), `tiween_logo-composition.pdf` (4pp), `reponses_brief_crea.pdf` (6pp), and 33 mockup PNGs are all present. Read directly. Findings below supersede the legacy text spec where they conflict.

### Logo / brand mark (from brand book + logo-composition)

- **Logomark:** circular **taa (ت) monogram** — a yellow-on-teal / teal-on-yellow disc reading as a smiley/face. Doubles as the app's avatar/favicon.
- **Wordmark:** Arabic **توين** (Tiween) — bold, geometric, custom Arabic letterforms.
- **Horizontal lockup:** `tiween.com` + monogram. In-app header uses the **yellow lockup centered on dark teal**.
- **Reversibility:** works teal-on-yellow, yellow-on-teal, white-on-teal, teal-on-white, white-on-blue.
- **Brand pattern:** tiled monogram (teal-on-white / teal-on-yellow) used as a textile/background motif.
- Designed by **Amen Okja / BLITS**.

### Palette — EXPANDED beyond legacy spec

- Legacy captured teal `#032523` + yellow `#F8EB06` only. Brand book + mockups also use:
  - **Cobalt blue** — vibrant secondary in marketing collateral (tees, mug, phone case backdrops). NOT used in product UI. Treat as marketing-only accent unless promoted.
  - A **lime/chartreuse yellow** variant + **near-black ink** appear on one logo-composition variant (more saturated pairing). Product UI uses the canonical `#F8EB06` yellow.
- **Decision:** product DESIGN.md palette = teal + canonical yellow + surfaces (as legacy). Cobalt blue logged as a marketing-tier color, available for editorial/promo surfaces, NOT default product chrome.

### Gaps RESOLVED by reading the real mockups

1. **Rounding** — cards ≈16px radius; showtime/format chips ≈8px; **primary CTA = full pill (fully rounded)**; bottom-sheet/modal large top radius. (Gap closed.)
2. **Elevation** — confirmed surface-color-shift system (no drop shadows in product). Cards = `#0A3533` on `#032523` field. (Gap closed.)
3. **Logo rules** — yellow lockup centered top on dark teal in-app; monogram as avatar/favicon. (Gap closed.)
4. **Icon set** — thin-line monochrome icons (home/search/ticket/account, map-pin, calendar, chevron, kebab ⋮). NOT emoji. BottomNav active = solid yellow. (Gap closed; legacy emoji were placeholders.)
5. **Poster aspect ratio** — **2:3 portrait** for film/event posters in feeds & carousels (legacy's "16:9" was wrong). Film-detail hero is a wide image strip with 01/05 pager. (Corrected.)
6. **Voice/tone** — marketing voice is **bold, playful, Tunisian-vernacular** ("tiween el forja el lilla?"). Product UI copy is **calm, clear, FR-default, reassuring** ("Merci de votre confiance !"). Two registers — see decision below.

### Product-skin observations (from mockups, govern in-app screens)

- **Dark, calm field**; yellow reserved for CTAs, active nav, "featured/recommended" ✲ star, links. Restraint is the rule in-product.
- Home (`Accueil`): top tabs `Tout / Cinéma / Musique`; horizontal carousels "Films à venir", "Films les mieux notés", "Sélection musique", "Podcasts les mieux notés"; portrait poster cards with rating badge.
- Film detail: title + director, meta chips (Action/Comédie, 2h17, date), **`Réserver` pill CTA** + `Voir la bande annonce` outline, wide image pager 01/05, Synopsis, Équipe artistique, Distribution (avatar row), **séances by venue** with date tabs (Aujourd'hui/Demain/dated) and format-badged showtime tiles.
- Ticketing: `Billetterie` → `Choix des billets` → showtime selection (venue cards, format-badged tiles, yellow ✲ on recommended) → disabled→active `Choisir cette séance` pill.
- Success: `Paiement validé !` + yellow check disc + `Retour à la page d'accueil` pill.
- My events (`Mes événements`): `À venir / Passés` tabs; ticket cards w/ poster, title, director, date, venue pin, qty × price in DT, `Télécharger mes billets` pill.
- BottomNav: `Accueil / Recherche / Billets / Compte`, thin-line icons, active=yellow.

## Decisions locked this session

- **Two voice registers:** Marketing = bold/playful/vernacular. Product UI = calm/clear/reassuring, FR-default. DESIGN.md Brand&Style documents both and states product governs in-app.
- **Palette tiers:** Core (teal/yellow/white/surfaces) = product. Cobalt blue = marketing/editorial accent, not product chrome.
- **Poster ratio = 2:3** in product feeds (corrects legacy 16:9).
- **CTA shape = full pill**; cards ~16px radius; chips ~8px.

## Design-improvement decisions (Ayoub approved — challenge invited)

Ayoub explicitly invited challenges to fonts/colors/layout. After reviewing the brand book + real mockups, three evolutions approved:

1. **Two-accent system (NEW — diverges from legacy + mockups).** Yellow `#F8EB06` was overloaded (CTA + active nav + recommended-star + links). Split:
   - **Yellow** = primary action + active/selected state ONLY.
   - **Cobalt blue** (brand secondary, from brand book) = links, info states, secondary highlights/tags.
   - Rationale: relieves a real signal-collision using an existing brand color; nothing invented.
2. **Lalezar reserved for brand moments (NEW — diverges from legacy "Lalezar all headlines").**
   - Lalezar → splash, hero, empty states, marketing, big brand numerals.
   - Routine in-app screen headers → cleaner heavy weight (Inter 700 / Cairo bold for AR) for scannability.
   - Rationale: heavy display face at UI sizes is shouty + hurts scanning when mixed with Inter body.
3. **Daylight treatment for outdoor-critical screens (NEW).**
   - App stays dark. **Ticket-QR screen and checkout get a light / high-brightness variant** for outdoor scanning (Tunisian daylight, cheap Android screens, 65% Chrome-Android audience).
   - Extends the legacy spec's "QR brightness-boost" hint into a fuller light pattern for those two surfaces.

**NOT changed (challenged but kept):** teal+yellow core palette (distinctive, AAA contrast, brand equity), taa-monogram logo, 2:3 poster-forward dark cards, conventional tab+carousel+bottom-nav layout (familiarity is a feature when displacing Facebook-page-checking; distinctiveness comes from content treatment — duotone cut-out portraits, monogram, Tunisian typography — not nav reinvention).

## Producer / handoff decision

- **Tool-agnostic producer prompt** (Ayoub undecided between Stitch & v0). Written to paste into either; notes on what each returns. Saved as `producer-prompt.md`.
- **EXPERIENCE.md drafted now, in parallel** (behavior already well-defined by legacy spec + mockups). Cross-refs DESIGN.md tokens by name.
- **DESIGN.md** written now as the locked spine the handoff will _refine_ (not replace) — it carries the approved 3 improvements so the tool inherits them.

## Finalization (2026-06-16)

- DESIGN.md + EXPERIENCE.md written, then validated by 3 parallel reviewers (coverage, accessibility, RTL/i18n) → `review-coverage.md`, `review-accessibility.md`, `review-rtl-i18n.md`.
- **CRITICAL/HIGH findings fixed in spines:**
  - Cobalt-as-link-text failed AA (2.3:1) → split into `secondary #2541E8` (fill, white text 7.0:1) + `link #8AB4FF` (text, ~6.4:1).
  - Mixed-script bidi unhandled → mandatory `<bdi>`/`dir="auto"` rule + `12,20 DT` currency format on EventCard & wherever AR+Latin share a line.
  - Selected showtime had no non-color cue → added check glyph ✓ + `aria-disabled` for sold-out.
  - Yellow focus ring invisible on yellow-filled controls → dark/white ring there.
  - Yellow CTA unreadable on white daylight surface → dark-teal CTA on daylight surfaces.
  - AR brand-moment font gap → Cairo 700/900 fallback for long AR copy; font chains declared.
  - Coverage: SeatSelector positioned as optional assigned-seating step; Theater/Concert/Venue detail reuse Film structure (deferred flows noted); time format `HH:MM` vs FR `15h00`.
- **MEDIUM/MINOR** triaged into the reviews + open-items (next Update pass).
- Both spines set `status: final`. Producer prompt corrected to inherit the fixed color rules.
- **Mock coverage:** no new HTML key-screens rendered this session — design-handoff mode defers screen rendering to the external tool (Stitch/v0). The 28 legacy mockups in `imports/` serve as the visual reference; spine-only surfaces will be rendered by the producer.

## Font challenge — second improvement round (2026-06-16, Ayoub approved)

Ayoub asked to challenge the font choices specifically. Role-by-role critique run against the brand book (geometric/rounded/warm wordmark DNA). Decisions:

- **Latin (body + headers): KEEP Inter.** Best small-size legibility on cheap Android/4G; free, variable. Brand voice carried by display/color/logo, not the workhorse.
- **Arabic body + headers: CHANGED Noto Sans Arabic → IBM Plex Sans Arabic.** Noto is the bland universal fallback; for an Arabic-first Tunisian product the AR face is the most brand-emotional typeface. Plex is geometric/warm/free, shares a humanist-geometric skeleton with Inter → AR+Latin on one line read as one system (reinforces the bidi rule). Also carries the AR header weight, so **Cairo's only remaining role is the Lalezar brand-moment fallback** for long AR copy.
- **Display: KEEP Lalezar for brand moments.** Already restrained; matches the Arabic wordmark energy; lowest churn.
- **Mono: KEEP JetBrains Mono** (no challenge — mono choice is immaterial for ticket IDs).
- Font chains: Latin `Inter` / `Lalezar → Inter` (display). Arabic `IBM Plex Sans Arabic` / `Lalezar → Cairo → IBM Plex Sans Arabic` (display).
- Applied to DESIGN.md (frontmatter + Typography table + chains), producer-prompt.md. EXPERIENCE.md unaffected (doesn't name the AR body font). Both spines remain `status: final`.

## Cobalt blue — value note

Exact cobalt hex not stated in brand book text; sampled visually from merch mockups as ~`#2541E8`–`#2B4FE0` range. Flagged `[NEEDS-HEX]` in DESIGN.md — to be pinned from the brand source vector or confirmed by Ayoub.

> _Mooted by the palette revision below — cobalt was dropped from the palette, so the `[NEEDS-HEX]` never needed pinning._

## Palette revision — Gold Leaf × Aubergine (2026-06-16, Ayoub approved) — SUPERSEDES the teal+yellow+cobalt decisions above

The B2C palette was revised wholesale from **Tiween-Green + Yellow + Cobalt** to **Gold Leaf × Aubergine**. This is the canonical palette of record; every teal/yellow/cobalt decision recorded earlier in this log is now historical.

**Direction:** _Aubergine Theatre with Gold Leaf Accent_ — a single deep-jewel dark theme, playbill/festival-catalogue register rather than streaming-app. A 2026-06-16 competitive scan (Letterboxd, Mubi, Dice, RA, Avignon, Teskerti, Fever) found film/event listings cluster in "safe poster-led dark"; the culture-defining brands (RA, Avignon) go boldly editorial. Tiween claims the open **culture-loud** quadrant, disciplined by category color-coding so the palette reads designed, not noisy.

**Why (rationale):**

- **Gold leaf is the matured, premium descendant of the brand yellow** — it carries the same warm-signal DNA but reads gilt/cultural rather than highlighter-bright, and (unlike yellow) it is legible as both fill and text.
- **Aubergine over teal** for a richer jewel-tone field that lets poster art lead.
- **Single-accent over two-accent:** gold now carries action/active/selected/recommended alone (one signal, no collision). Cobalt is **dropped entirely** — this resolves the CRITICAL cobalt-link AA failure (2.3:1) by removing the offending token rather than splitting it.

**Tokens pinned (all ratios computed against the field/surface in DESIGN.md):**

- Field `background #241326` (Midnight Aubergine) · `foreground #FFFFFF` (17.6:1) · `surface #31203A` · `surface-raised #3E2A48` · `border #4A3556` · `muted-foreground #B0A6B8` (7.5:1).
- `primary #D4A24A` (Gold Leaf — action/active/selected/✲); fill takes dark ink `primary-foreground #2A1A06` (7.3:1) — **never white on gold** (2.3:1). Gold as text on field = 7.6:1.
- `gold-tint / link #E0B563` for link/info/secondary text (9.2:1).
- `secondary #E5478A` (Magenta Rose) — secondary/category accent + théâtre, **not** a second action color.
- Semantic retuned for the plum field: `destructive #FF8A8A` (7.7:1), `success #5BD08A` (9.1:1), `warning #E8B24C`.
- **Daylight CTA changed** dark-teal → **aubergine `#241326` fill + white text** (gold on white ≈ 1.6:1 fails).
- **Category color-coding** added: cinema `#E0B563` · théâtre `#E5478A` · courts `#5FD0C2` (teal cameo of the retired green DNA) · musique `#7B9CFF` · expo `#C98AE8`.

**Logo:** gilt taa-monogram (gold-on-aubergine / aubergine-on-gold), matured form of the original yellow-on-teal mark.

**Applied to:** DESIGN.md (frontmatter + Colors + all token refs), EXPERIENCE.md (Open items: old cobalt `[NEEDS-HEX]` marked resolved/moot), producer-prompt.md (identity block + daylight CTA + focus rings + "what to resolve" re-aimed at aubergine-era questions). Prior-palette reviews (`review-accessibility.md`, `review-coverage.md`) banner-marked SUPERSEDED — their contrast ratios were computed against retired hexes; DESIGN.md is the source of truth for current ratios. Both spines remain `status: final`.
