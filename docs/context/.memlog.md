---
updated: 2026-08-12T16:02
---

- (event) first run: ingest intent, interactive mode
- (decision) bundle root = docs/context/ (separate from hand-written docs/ files); set via project_knowledge in _bmad/custom/config.toml
- (decision) context_placement = both (BMad arrays + managed block in AGENTS.md)
- (decision) scope = whole repo, repo-only sources; no external docs supplied
- (fact) top-level project_knowledge override in _bmad/custom/config.toml leaves modules.bmm.project_knowledge at docs/ — other BMad skills unaffected
- (event) 5 parallel scanners returned: commands/tooling, strapi backend, client frontend, landmines, bmad process
- (finding) no Playwright dep and no test:e2e script anywhere; project-context.md:255 documents 'yarn test:e2e' which does not exist
- (finding) strapi yarn test = jest, runs ONLY *.unit.test.ts; *.service.test.ts/*.controller.test.ts run via yarn test:integration and are absent from CI (.github/workflows/ci.yml)
- (finding) client yarn test = vitest run; strapi = jest — the two apps use different runners; project-context.md:137 says 'Use Vitest not Jest' globally
- (finding) strapi tests use __tests__/ dirs (69 files) vs 17 co-located; client has ZERO __tests__ dirs and 113 co-located — project-context.md anti-pattern #5 'never create __tests__' holds only for client
- (finding) i18n messages live at apps/client/locales/{ar,en,fr}.json, not src/messages/ as project-context.md:113 states
- (finding) architecture.md:442 marks entity-properties plugin DELETED but apps/strapi/src/plugins/entity-properties/ exists and is wired
- (finding) AGENTS.md:18-19 story workflow (create-story/dev-story/code-review) is stale; artifacts are spec-*.md with bmad-build/bmad-spec shape
- (finding) R1-R5 plugin dependency rules are review-only; no eslint no-restricted-paths/boundaries rule exists in apps/strapi/eslint.config.mjs or packages/eslint-config
- (finding) docs/strapi-admin-credentials.md is tracked and contains a plaintext admin email+password
- (finding) node pinned 22.21.1 at root .nvmrc/.tool-versions but 22.22.0 in apps/*/.tool-versions
- (finding) prettier pinned 3.3.2 deliberately; bumping to 3.3.3+ breaks Strapi generated TS schemas (package.json comments field)
- (finding) request-auth.ts:29 allow-list comment: do NOT add api/ticketing/orders — prefix match would re-admit a route story 6.3 review deliberately dropped
- (finding) progress.json/status.json at root are gitignored, unreferenced by any repo file — leftovers from a non-bmad outer loop
- (confirmed) test layout is PER-APP: client co-located (no __tests__), strapi __tests__/ dirs — both intentional
- (confirmed) no E2E suite exists; Playwright in project-context.md stack table is aspirational
- (confirmed) current implementation workflow is bmad-build / bmad-build-auto; AGENTS.md three-skill cycle is stale
- (confirmed) docs/strapi-admin-credentials.md = local dev seed only, safe; never reuse for staging/prod
- (confirmed) apps/client/src/prototypes/ = design prototypes pending promotion into features/, not production
- (confirmed) node pin 22.21.1 (root) vs 22.22.0 (apps) is unintended drift — record as landmine, neither is authoritative
- (confirmed) user: nothing further missing; proceed to write
- (finding) entity-properties verdict VESTIGIAL: registered in config/plugins.ts but content-types/services/controllers/routes all 'export default {}'; types migrated to venues plugin; deletion is story 2C.5
- (event) wrote kernel (33 instructions, 905 tokens), 10 entries, 1 compass (apps/strapi); bundle 2639 tokens
- (event) fresh-eyes pruning pass applied: kernel cut 42->33 instructions; removed derivable lines (types/generated, feature barrels, admin policies, prototypes ban, node pin drift, openspec dup, bmad-build dup, request-auth allow-list)
- (correction) pruning pass caught a factual error in 3 files: strapi 'yarn test' runs BOTH jest projects (server *.unit.test.ts AND admin *.test.tsx); only boot-based suites are opt-in. Verified against apps/strapi/jest.config.cjs
- (finding) apps/client/src/prototypes/ holds only 12 .html files + netlify.toml — nothing importable, so a kernel ban was pointless; dropped
- (decision) synced kernel into AGENTS.md and compass into apps/strapi/AGENTS.md (managed bmad:context blocks)
- (decision) AGENTS.md workflow line corrected to bmad-build/bmad-build-auto; user chose to leave rest of AGENTS.md and CLAUDE.md untouched
- (decision) _bmad-output/project-context.md replaced with a redirect stub to docs/context/; its wrong rules recoverable via git log
- (decision) added persistent_facts = file:{project-root}/docs/context/kernel.md to _bmad/custom/bmad-build.toml and bmad-build-auto.toml (both-placement, BMad arrays side)
- (landmine) prettier BREAKS the bundle: it re-wraps frontmatter sources arrays across lines (context.py parser is flat-YAML only) and rewrites the append-only .memlog.md — docs/context/** added to .prettierignore; never remove it
- (gap) node pin drift (root 22.21.1 vs apps 22.22.0) is unresolved drift, deliberately kept out of the kernel — align the .tool-versions files
- (gap) R1-R5 plugin dependency rules have no mechanical enforcement; architecture.md:660 lists a CI lint as unbuilt
- (event) ingest complete: validate clean, sweep clean
